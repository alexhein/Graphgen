package de.alexhein.graphgenerator.graph.datastructures;

import java.util.Set;

public class CliqueIntersectionGraph<T> extends UndEdgeGraphHashed<T, T>
		implements UndirectedGraphEdged<T, T> {

		CliqueIntersectionGraph(UndirectedGraph<T> g){
		
		}
}
